{
  "name": "mightyloka",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "cp CNAME dist/ && npx gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^5.8.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
